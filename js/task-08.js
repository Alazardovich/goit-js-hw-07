
const inputElement = document.querySelector('input');
const buttonAddElement = document.querySelector('button[data-action="render"]');
const buttonRemoveElement = document.querySelector('button[data-action="destroy"]');

inputElement.addEventListener('keypress', createBoxes);
buttonRemoveElement.addEventListner('click', onDeleteButton);
buttonAddElement.addEventListener('click', onCreatButton);

function createBoxes(amount) {
console.log(amound);
}
function onCreatButton(){
const divEl = document.createElement(div);
 document.body.appendChild  
}
function onDeleteButton(){

}
